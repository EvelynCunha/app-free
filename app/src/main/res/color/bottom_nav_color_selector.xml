<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cor para o item selecionado (estado checked) -->
    <item android:color="@color/blue" android:state_checked="true"/>
    <!-- Cor para os itens não selecionados (estado padrão) -->
    <item android:color="@color/light_blue" android:state_checked="false"/>
</selector>